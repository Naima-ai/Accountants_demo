pydantic>=2.0
PyMuPDF>=1.23
pdfplumber>=0.10
pandas>=2.0
lxml>=4.9
Pillow>=10.0
pytesseract>=0.3.10
requests>=2.31.0
json-repair>=0.25.0
datasets>=2.14.0
huggingface_hub>=0.19.0
Faker>=25.0
# Local SLM: in-process llama.cpp, replaces the old Ollama daemon client.
# On Windows (confirmed on this repo's dev box, Python 3.12), plain
# `pip install -r requirements.txt` fails building llama-cpp-python from
# source (no PyPI wheel for this platform/Python combo). Install it
# separately first, from the project's prebuilt CPU wheel index, THEN
# install the rest of this file:
#   pip install llama-cpp-python --prefer-binary --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
llama-cpp-python>=0.2.90
# Shared stack (src/): database, memory, orchestration, API
sqlalchemy>=2.0
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9

# Optional: PDF export for Demo 3 advisory letters (src/agents/report_agent.py
# falls back to text-only if this isn't installed)
reportlab>=4.0
